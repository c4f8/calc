# Decision Log

2026-06-08: Created the BMad-native governance and memory migration spec from `docs/project-context.md`, `CONTEXT.md`, `docs/production-launch-checklist.md`, `docs/superpowers/**`, `_bmad` config, and git status. Coherence pass: the kernel has six stable capabilities with intent and success, explicit constraints, non-goals, and testable success signal. Preservation pass: product truth, security/privacy invariants, legacy memory disposition, installed BMad paths, dirty-worktree constraints, migration steps, verification checks, risks, and open questions are represented in `SPEC.md`, `memory-map.md`, or `migration-plan.md`; wrapper-only legacy `superpowers:*` execution ceremony is intentionally not preserved as future authority.
