# Decision Log

## 2026-06-08

- Created `SPEC-express-individual-goods-split` from the user request, client-selected mockup image, `docs/project-context.md`, and current code evidence.
- Interpreted the mockup as a surgical update to the existing ARCHIPELAG UI, not permission to redesign the product.
- Assumed one pricing basis per **Good** shared across modes, with separate numeric prices for Express and Individual, because the chosen admin mockup shows one `Режим` and two price fields.
- Logged unresolved product decisions as open questions in `SPEC.md`.
- Self-validation verdict: Spec Law pass. Each capability has intent and success; constraints rule out redesign, scope creep, and unsafe admin changes; non-goals are explicit; success signal is demonstrable. Preservation pass captured all load-bearing source claims from the request, mockup, project context, and code evidence into `SPEC.md` or companions. Wrapper-only content dropped: prompt ceremony and image-generation discussion that did not change the build contract.
